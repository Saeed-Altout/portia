import { HeroCom } from "@/components/ui/hero";

export const Hero = () => {
  return (
    <HeroCom
      title="Pricing plans"
      description="Transparent pricing that grows with your needs."
    />
  );
};
